# 급식 배틀 - 학교 급식 조회 앱

## 1. 문서 목적

이 문서는 사용자가 학교와 날짜 범위를 선택해 NEIS가 제공하는 중식 정보를
조회하는 웹 애플리케이션의 제품 요구사항을 정의한다. 구현 방법의 상세 내용은
[`TRD.md`](TRD.md)에서 다룬다.

## 2. 개발 목적

NEIS 급식 정보는 학교 식단을 확인할 수 있는 공공 데이터이지만, 사용자가 학교
식별 코드나 API 구조를 알지 못하면 직접 활용하기 어렵다. 이 제품은 학교 이름과
날짜만으로 중식 정보를 쉽게 찾을 수 있는 일관된 사용자 경험을 제공한다.

### 목표

- 학교 이름의 일부만 입력해 원하는 학교를 찾고 선택할 수 있다.
- 시작일과 종료일을 지정해 여러 날짜의 중식 정보를 한 번에 조회할 수 있다.
- 조회한 급식 정보를 날짜순으로 읽기 쉽게 확인할 수 있다.
- 조회 실패와 데이터가 없는 상태를 사용자가 구분하고 다음 행동을 선택할 수
  있게 한다.

### 범위에서 제외하는 항목

- 사용자의 회원 가입, 로그인 및 개인화
- 조식과 석식 조회
- 급식 만족도 평가, 학교 간 비교 및 AI 분석
- 급식 정보의 생성, 수정 또는 삭제
- NEIS가 제공하지 않는 정보의 추정

## 3. 대상 사용자

- 본인 또는 자녀가 다니는 학교의 급식을 확인하려는 학생과 보호자
- 학교 급식 일정을 확인하려는 교직원
- 특정 학교의 공개 급식 정보를 간편하게 조회하려는 일반 사용자

사용자가 학교의 교육청 코드나 표준학교 코드를 알지 못한다는 점을 기본 전제로
한다.

## 4. 핵심 사용자 흐름

사용자는 다음 세 단계를 순서대로 수행한다.

1. **학교 검색 및 선택**: 학교 이름의 일부를 입력해 검색하고, 검색 결과에서 한
   학교를 선택한다.
2. **날짜 범위 선택**: 조회 시작일과 종료일을 선택한다.
3. **급식 결과 확인**: 선택한 범위의 중식 정보를 날짜별로 확인한다.

학교를 다시 선택하거나 날짜 범위를 변경하면 새 조건으로 다시 조회할 수 있다.
각 단계에서는 현재 선택값과 다음 단계로 진행하기 위해 필요한 입력을 명확히
표시한다.

## 5. 기능 요구사항

### FR-1. 부분 학교명 검색

- 사용자는 학교 이름의 일부를 검색어로 입력할 수 있다.
- 검색어의 앞뒤 공백은 검색 전에 제거한다.
- 빈 검색어로는 검색을 요청할 수 없다.
- 검색 중임을 알 수 있는 상태를 표시하고, 동일한 검색을 중복 요청하지 않는다.
- 검색 결과에는 사용자가 동명의 학교를 구분할 수 있도록 학교명과 함께 NEIS가
  제공하는 지역 또는 주소 정보를 표시한다.

### FR-2. 학교 선택

- 검색 결과에서 한 학교만 선택할 수 있다.
- 선택한 학교를 명확히 표시한다.
- 학교를 변경하면 이전 학교의 급식 결과를 지우고 새 학교에 맞는 조회 조건을
  적용한다.
- 날짜 선택 단계는 학교를 선택한 후에만 진행할 수 있다.

### FR-3. 날짜 범위 선택

- 사용자는 시작일과 종료일을 각각 선택할 수 있다.
- 시작일과 종료일을 모두 입력해야 조회할 수 있다.
- 시작일은 종료일보다 늦을 수 없다.
- 날짜는 사용자의 현지 달력 기준으로 선택하되, 서버에는 날짜만 전달해 시간대
  변환으로 날짜가 바뀌지 않게 한다.

### FR-4. 중식 조회

- 선택한 학교와 날짜 범위로 급식 정보를 조회한다.
- 조회 대상 식사 유형은 중식으로 한정한다.
- 결과는 날짜 오름차순으로 표시한다.
- 각 날짜의 결과에는 최소한 급식 일자와 메뉴를 표시한다.
- 열량, 영양정보, 원산지 등 NEIS 응답에 추가 정보가 있으면 항목명과 함께
  표시하며, 값이 없는 항목을 추정하지 않는다.
- NEIS가 메뉴에 포함한 알레르기 표기 등 원문 정보는 의미가 손실되지 않도록
  표시한다.

### FR-5. 상태 및 오류 안내

- 최초 화면, 검색 전, 검색 중, 조회 중, 결과 있음, 결과 없음, 오류 상태를
  시각적으로 구분한다.
- 오류 메시지는 원인을 이해할 수 있는 문구와 재시도 방법을 제공한다.
- 네트워크 또는 서버 오류가 발생해도 사용자가 입력한 학교와 날짜 범위를
  유지해 재시도할 수 있게 한다.

## 6. 예외 및 경계 상황

| 상황 | 기대 동작 |
|------|-----------|
| 검색어가 비어 있음 | 검색 요청을 보내지 않고 학교 이름 입력을 안내한다. |
| 검색 결과가 없음 | 일치하는 학교가 없음을 알리고 다른 검색어를 제안한다. |
| 동명의 학교가 여러 개임 | 지역 또는 주소를 함께 표시해 사용자가 직접 선택하게 한다. |
| 시작일 또는 종료일이 없음 | 누락된 날짜를 표시하고 조회를 비활성화한다. |
| 시작일이 종료일보다 늦음 | 올바른 날짜 범위를 안내하고 조회 요청을 보내지 않는다. |
| 선택 범위에 중식 정보가 없음 | 오류가 아닌 빈 결과로 처리하고 급식 정보가 없음을 표시한다. |
| 일부 날짜에만 정보가 없음 | 데이터가 있는 날짜는 표시하고, 없는 날짜를 임의의 메뉴로 채우지 않는다. |
| NEIS 또는 백엔드 요청 실패 | 일반적인 빈 결과와 구분되는 오류를 표시하고 재시도를 제공한다. |
| 응답에 선택적 정보가 없음 | 해당 항목을 생략하거나 정보 없음으로 표시하고 값을 추정하지 않는다. |

## 7. 비기능 요구사항

### 사용성 및 접근성

- 학교 검색, 날짜 선택, 결과 영역은 키보드만으로 사용할 수 있어야 한다.
- 입력 요소에 명확한 레이블을 제공하고 상태 및 오류 변경을 보조 기술이 인식할
  수 있어야 한다.
- 모바일과 데스크톱 화면에서 핵심 흐름을 완료할 수 있어야 한다.

### 성능

- 검색 및 급식 조회 요청이 진행 중임을 즉시 표시한다.
- 사용자 입력으로 연속 검색이 발생할 때 불필요한 요청을 줄인다.
- 한 번의 조회 결과를 날짜별 추가 요청 없이 표시할 수 있어야 한다.

### 신뢰성 및 보안

- 프론트엔드는 NEIS API를 직접 호출하지 않는다.
- 외부 API 오류를 성공 또는 결과 없음으로 위장하지 않는다.
- 사용자 입력과 외부 API 응답은 신뢰하지 않고 애플리케이션 경계에서 검증한다.
- API 키 등 비밀값을 브라우저 번들이나 저장소에 포함하지 않는다.

## 8. 성공 기준

- 사용자가 학교 코드를 알지 못해도 부분 학교명 검색부터 중식 결과 확인까지
  세 단계를 완료할 수 있다.
- 정상 응답, 검색 결과 없음, 급식 정보 없음, 잘못된 날짜 범위 및 요청 실패를
  서로 구분해 안내한다.
- 프론트엔드가 백엔드를 통해서만 NEIS 데이터를 조회한다.
- 지원하는 화면 크기와 키보드 조작 환경에서 핵심 흐름을 완료할 수 있다.

## 9. 인수 기준

1. 학교 이름 일부로 검색하면 일치하는 학교 목록이 표시되고 한 학교를 선택할
   수 있다.
2. 학교 선택 전에는 날짜 범위 조회를 진행할 수 없다.
3. 유효한 시작일과 종료일을 선택하면 해당 범위의 중식이 날짜순으로 표시된다.
4. 시작일이 종료일보다 늦으면 외부 요청 없이 입력 오류가 표시된다.
5. 학교 검색 결과와 급식 조회 결과가 없을 때 각각 구분된 빈 상태가 표시된다.
6. 백엔드 또는 NEIS 장애는 빈 결과와 구분된 오류로 표시되고 재시도할 수 있다.
7. 브라우저는 NEIS API에 직접 요청하지 않는다.

