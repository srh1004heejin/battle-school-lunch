# 급식 배틀 - 학교 급식 조회 제품 요구사항

## 1. 문서 목적

이 문서는 NEIS 공개 데이터를 이용해 학교와 날짜 범위별 중식을 조회하는 제품의
요구사항을 정의한다. 제품은 사람이 사용하는 웹 애플리케이션과 AI 에이전트가
사용하는 MCP 서버를 제공한다. 구현 상세는 [`TRD.md`](TRD.md)에서 다룬다.

## 2. 제품 목표와 범위

NEIS 급식 정보는 학교 식별 코드와 API 구조를 알아야 직접 활용하기 쉽다. 이
제품은 사용자가 학교 이름과 날짜만으로 급식을 확인하고, AI 에이전트도 동일한
공개 정보를 명확한 도구 계약으로 조회할 수 있게 한다.

### 목표

- 부분 학교명으로 후보 학교와 올바른 식별 정보를 찾는다.
- 선택한 학교와 날짜 범위의 중식을 날짜순으로 조회한다.
- 웹과 MCP 양쪽에서 데이터 없음, 잘못된 입력, 외부 장애를 구분한다.
- NEIS API 키를 브라우저, MCP 입력·출력 또는 오류에 노출하지 않는다.

### 범위에서 제외하는 항목

- 회원 가입, 로그인 및 개인화
- 조식과 석식 조회
- 급식 만족도 평가, 학교 간 비교 및 AI 분석
- 급식 정보의 생성, 수정 또는 삭제
- NEIS가 제공하지 않는 정보의 추정
- 공개 인터넷 MCP 인증 및 권한 관리

## 3. 대상 사용자

- 본인 또는 자녀가 다니는 학교의 급식을 확인하는 학생과 보호자
- 학교 급식 일정을 확인하는 교직원과 일반 사용자
- 학교 급식 정보가 필요한 MCP 호환 AI 에이전트와 그 운영자

사람과 에이전트 모두 교육청 코드나 표준학교 코드를 사전에 알지 못할 수 있다.

## 4. 핵심 사용자 흐름

### 4.1 웹 사용자

1. 학교 이름 일부를 입력해 후보를 검색한다.
2. 지역과 주소를 확인해 한 학교를 선택한다.
3. 시작일과 종료일을 지정해 날짜별 중식을 확인한다.

### 4.2 MCP 클라이언트

1. Streamable HTTP 엔드포인트에 연결하고 도구 목록을 조회한다.
2. `search_schools`로 후보 학교와 교육청·학교 식별 정보를 얻는다.
3. 식별 정보와 날짜 범위를 `get_lunch_meals`에 전달한다.
4. 구조화된 중식 결과를 사용하거나 표준 도구 오류에 따라 입력 수정 또는 재시도
   여부를 결정한다.

## 5. 기능 요구사항

### FR-1. 부분 학교명 검색

- 앞뒤 공백을 제거한 비어 있지 않은 검색어를 사용한다.
- 결과에는 학교명, 교육청 코드·이름, 학교 코드와 가능한 지역·주소를 포함한다.
- 동명 학교를 식별 코드와 지역 정보로 구분할 수 있어야 한다.
- 웹은 검색 중 상태를 표시하고 동일 요청의 불필요한 중복을 줄인다.
- MCP는 결과가 없을 때 성공한 빈 결과가 아닌 명확한 `SCHOOL_NOT_FOUND` 도구
  오류를 반환한다.

### FR-2. 학교 선택

- 웹에서는 한 학교만 선택하고 학교 변경 시 이전 급식 결과를 지운다.
- 학교 식별에는 교육청 코드와 학교 코드 조합을 사용한다.
- MCP 클라이언트는 검색 도구가 반환한 두 코드를 급식 도구에 전달할 수 있다.

### FR-3. 날짜 범위

- 시작일과 종료일은 실제 `YYYY-MM-DD` 달력 날짜여야 한다.
- 시작일은 종료일보다 늦을 수 없고 설정된 최대 조회 기간을 넘을 수 없다.
- 날짜는 시간대 변환 없이 날짜 값으로 NEIS에 전달한다.
- 잘못된 입력은 NEIS 요청 전에 차단한다.

### FR-4. 중식 조회

- 식사 유형은 NEIS 중식 코드 `2`로 고정한다.
- 결과는 날짜 오름차순이며 급식 일자와 메뉴를 반드시 포함한다.
- 열량, 영양정보, 원산지는 값이 있을 때만 반환하며 값을 추정하지 않는다.
- 메뉴의 알레르기 표기 의미를 보존하고 외부 HTML은 안전한 텍스트로 정규화한다.
- MCP는 급식이 없을 때 `MEALS_NOT_FOUND` 도구 오류를 반환한다.

### FR-5. MCP 연결과 도구 계약

- MCP 서버는 기존 백엔드와 별도 프로세스 및 컨테이너로 실행한다.
- 공식 Python MCP SDK 1.x와 Streamable HTTP 전송을 사용한다.
- 클라이언트는 초기화 후 `search_schools`, `get_lunch_meals` 목록을 조회하고
  각 도구의 설명과 JSON 입력 스키마를 확인할 수 있다.
- 성공 호출은 텍스트 콘텐츠와 구조화 콘텐츠를 함께 제공한다.
- 실패 호출은 `isError: true`, 안정적인 오류 코드와 사용자에게 안전한 메시지를
  제공한다.

### FR-6. 상태와 오류

- 웹은 최초, 검색 중, 조회 중, 결과, 빈 결과, 오류 상태를 구분한다.
- MCP는 입력 오류, 검색 결과 없음, 급식 없음, NEIS 잘못된 응답, 연결 실패,
  상류 오류와 시간 초과를 구분한다.
- 외부 오류를 성공이나 데이터 없음으로 위장하지 않는다.
- 오류에는 API 키, 외부 URL 쿼리, 내부 예외 또는 민감한 응답 원문을 포함하지
  않는다.

## 6. 비기능 요구사항

### 사용성과 상호운용성

- 웹의 핵심 흐름은 키보드와 모바일·데스크톱 화면에서 사용할 수 있어야 한다.
- MCP 도구 입력과 출력은 클라이언트가 추가 파싱 규칙 없이 사용할 수 있는
  구조화 데이터여야 한다.
- MCP 서버는 헬스 엔드포인트를 제공하고 Docker Compose에서 준비 상태를 확인한다.

### 성능과 신뢰성

- 한 번의 급식 조회로 지정 범위 결과를 반환하고 NEIS 페이지네이션을 처리한다.
- 외부 요청에 연결 및 전체 타임아웃을 적용한다.
- 연결 장애와 일시적 서버 오류만 제한적으로 재시도한다.
- 웹 API와 MCP 서버 장애는 서로의 프로세스 실행을 직접 중단시키지 않는다.

### 보안

- 프론트엔드는 NEIS API를 직접 호출하지 않는다.
- NEIS API 키는 서버 환경 변수로만 주입한다.
- 사용자 입력과 NEIS 응답을 애플리케이션 경계에서 검증한다.
- MCP 서버를 인증 없이 공개 인터넷에 노출하는 구성은 범위에 포함하지 않는다.

## 7. 인수 기준

1. 학교 이름 일부로 웹 검색과 MCP 도구 호출에서 후보 학교를 식별할 수 있다.
2. 유효한 학교와 날짜 범위로 중식이 날짜순 구조화 결과로 반환된다.
3. MCP 클라이언트가 Streamable HTTP 서버에 연결해 두 도구 목록을 조회할 수 있다.
4. 잘못된 검색어, 식별자, 날짜 및 범위는 NEIS 호출 전에 명확한 오류가 된다.
5. 학교 검색 결과 없음과 급식 정보 없음을 서로 다른 MCP 도구 오류로 구분한다.
6. NEIS 오류, 잘못된 응답, 연결 실패와 시간 초과를 안전한 오류로 구분한다.
7. API 키나 내부 예외 정보가 웹 또는 MCP 출력에 포함되지 않는다.
8. MCP 컨테이너가 프론트엔드 및 백엔드와 함께 Docker Compose로 실행된다.
9. 웹·백엔드의 기존 기능과 MCP 단위·통합 테스트가 모두 통과한다.
