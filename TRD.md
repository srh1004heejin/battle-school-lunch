# 급식 배틀 - 학교 급식 조회 앱 기술 요구사항

## 1. 문서 목적

이 문서는 [`PRD.md`](PRD.md)의 제품 요구사항을 구현하기 위한 시스템 구조,
구성 요소의 책임, API 계약, 데이터 흐름, 실행 환경 및 테스트 전략을 정의한다.
이 문서에서 설명하는 디렉터리와 기술 구성은 구현 단계에서 생성할 목표
구조이며, 현재 저장소에 구현되어 있음을 의미하지 않는다.

## 2. 기술 원칙과 제약

- 애플리케이션은 React 프론트엔드, Python 백엔드와 독립 Python MCP 서버로 분리한다.
- 프론트엔드는 NEIS API를 직접 호출하지 않고 백엔드의 내부 API만 호출한다.
- 백엔드는 NEIS API 연동과 외부 응답의 검증 및 변환을 담당한다.
- 외부 NEIS API의 기준 계약은 `data/openapi.json`으로 관리한다.
- 프론트엔드와 백엔드 사이의 기준 계약은 `src/openapi.json`으로 별도 관리한다.
- 두 계약의 클라이언트 코드는 분리하고 각 명세에서 생성하거나 명세에 맞춰
  구현한다. 외부 API 모델을 프론트엔드에 그대로 노출하지 않는다.
- 프론트엔드와 백엔드는 Docker 이미지로 빌드하고 Docker Compose로 함께
  실행한다.
- MCP 서버는 공식 Python MCP SDK 1.x의 Streamable HTTP 전송을 사용하고 NEIS를
  직접 호출하되 백엔드 프로세스에 의존하지 않는다.
- 비밀값은 환경 변수로 주입하고 이미지, 소스 코드 및 브라우저 번들에 포함하지
  않는다.

`data/openapi.json`이 구현 시작 시점에 없다면 먼저 이전 워크숍 단계의 산출물을
준비하고 명세를 검증해야 한다. 실제 NEIS 경로, 필드명 및 필수 매개변수는 이
명세를 최종 기준으로 삼는다.

## 3. 목표 시스템 구조

```text
Browser
  |
  | HTTP/JSON (src/openapi.json)
  v
React frontend
  |
  | Docker Compose internal/public network
  v
Python backend
  |
  | HTTPS/JSON (data/openapi.json)
  v
NEIS Open API

MCP client
  |
  | Streamable HTTP
  v
Python MCP server
  |
  | HTTPS/JSON (data/openapi.json)
  v
NEIS Open API
```

### 목표 디렉터리

```text
.
├── data/
│   └── openapi.json          # NEIS 외부 API 계약
├── frontend/                 # React UI 및 내부 API 클라이언트
├── backend/                  # Python API 및 NEIS 클라이언트
├── src/
│   ├── mcp/                  # 독립 MCP 서버 및 테스트
│   └── openapi.json          # 프론트엔드-백엔드 내부 API 계약
└── compose.yaml              # 로컬 통합 실행 구성
```

구체적인 하위 구조와 패키지 버전은 구현 시점의 저장소 규칙과 호환되는 범위에서
결정한다. 계약 파일과 생성 클라이언트는 어느 API를 대상으로 하는지 이름과
모듈 경계로 명확히 구분한다.

## 4. 구성 요소별 책임

### 4.1 React 프론트엔드

프론트엔드는 다음을 담당한다.

- 학교 검색, 학교 선택, 날짜 범위 선택 및 결과 표시의 3단계 UI
- 빈 입력과 잘못된 날짜 범위에 대한 요청 전 검증
- 검색 중, 조회 중, 빈 결과, 성공 및 오류 상태 표현
- `src/openapi.json`을 따르는 백엔드 API 클라이언트 호출
- 내부 API 응답을 화면 모델로 변환하고 접근 가능한 형태로 렌더링
- 연속 검색 시 디바운스 또는 취소 가능한 요청으로 불필요한 호출 방지
- 새 검색이나 조회가 시작될 때 이전 요청의 늦은 응답이 최신 상태를 덮지 않도록
  요청 수명 주기 관리

프론트엔드는 NEIS URL, NEIS API 키 및 `data/openapi.json` 기반 외부 클라이언트를
포함하지 않는다.

### 4.2 Python 백엔드

백엔드는 다음을 담당한다.

- `src/openapi.json`에 정의된 학교 검색 및 급식 조회 엔드포인트 제공
- 요청 매개변수의 형식과 날짜 범위 검증
- `data/openapi.json`을 따르는 전용 NEIS 클라이언트 호출
- NEIS 응답의 상태 코드와 본문 스키마 검증
- 외부 학교 및 급식 데이터를 안정적인 내부 응답 모델로 변환
- 외부 서비스의 결과 없음과 장애를 구분해 내부 API 상태로 매핑
- 타임아웃을 적용하고 안전한 범위에서 연결 오류와 일시적 서버 오류만 제한적으로
  재시도
- 요청 추적에 필요한 로그를 남기되 API 키와 개인정보는 기록하지 않음

백엔드는 외부 API의 예외 형식이나 필드 변경이 프론트엔드에 직접 전파되지 않게
어댑터 경계를 유지한다.

### 4.3 Python MCP 서버

- `/mcp`에서 stateless Streamable HTTP를 제공한다.
- `search_schools`, `get_lunch_meals` 도구와 JSON 입력 스키마를 등록한다.
- `data/openapi.json`의 학교 및 급식 경로와 필드를 기준으로 NEIS를 호출한다.
- 입력, 외부 응답과 날짜 범위를 독립적으로 검증한다.
- 성공 결과는 구조화 콘텐츠로, 실패는 `isError: true`인 안정적인 코드와
  메시지로 반환한다.
- API 키, 외부 요청 쿼리 및 내부 예외 상세를 MCP 응답에 포함하지 않는다.

## 5. API 계약

### 5.1 외부 NEIS API

`data/openapi.json`은 학교 기본정보 검색과 학교 급식식단정보 조회에 필요한
경로, 쿼리 매개변수 및 응답 스키마의 단일 기준이다. 백엔드는 명세에서 확인한
교육청 코드, 학교 코드, 급식 일자 및 식사 유형 필드를 사용한다.

- 학교 검색 요청에는 정리된 부분 학교명을 전달한다.
- 급식 요청에는 선택한 학교의 식별 코드, 시작일, 종료일 및 중식 조건을
  전달한다.
- 명세의 페이지 크기 제한 때문에 결과가 여러 페이지라면 백엔드가 필요한
  페이지를 수집한 후 내부 모델로 반환한다.
- NEIS의 성공 본문 안에 포함된 오류 코드도 실패 여부를 판정해 처리한다.
- 응답 필드는 사용 전에 런타임에서 검증하며, 필수 식별자가 없는 레코드는
  성공 데이터로 반환하지 않는다.

### 5.2 내부 백엔드 API

내부 계약은 구현 단계에서 `src/openapi.json`에 OpenAPI 형식으로 작성한다. 최소
계약은 다음과 같다. 실제 경로를 변경할 경우 두 애플리케이션과 계약 테스트를
동시에 갱신한다.

#### 학교 검색

`GET /api/schools?query={부분 학교명}`

성공 응답:

```json
{
  "schools": [
    {
      "educationOfficeCode": "교육청 코드",
      "schoolCode": "학교 코드",
      "name": "학교명",
      "region": "지역 또는 관할 교육청",
      "address": "주소"
    }
  ]
}
```

- `query`는 공백 제거 후 비어 있으면 `400`을 반환한다.
- 검색 결과가 없으면 `200`과 빈 `schools` 배열을 반환한다.
- 학교 식별에는 학교 코드만 단독으로 사용하지 않고 교육청 코드와 학교 코드의
  조합을 사용한다.

#### 중식 조회

`GET /api/schools/{educationOfficeCode}/{schoolCode}/meals?from={YYYY-MM-DD}&to={YYYY-MM-DD}&mealType=lunch`

성공 응답:

```json
{
  "school": {
    "educationOfficeCode": "교육청 코드",
    "schoolCode": "학교 코드",
    "name": "학교명"
  },
  "from": "YYYY-MM-DD",
  "to": "YYYY-MM-DD",
  "meals": [
    {
      "date": "YYYY-MM-DD",
      "mealType": "lunch",
      "menu": ["메뉴 1", "메뉴 2"],
      "calories": "선택적 원문 값",
      "nutrition": {},
      "origin": "선택적 원문 값"
    }
  ]
}
```

- 경로 코드와 날짜는 형식 및 허용 범위를 검증한다.
- `from`이 `to`보다 늦으면 `400`을 반환한다.
- 급식 정보가 없으면 `200`과 빈 `meals` 배열을 반환한다.
- 결과는 `date` 오름차순이며 동일 날짜의 중복 레코드를 안정적으로 처리한다.
- 메뉴 구분자와 마크업 변환 규칙은 백엔드에서 일관되게 적용하되 알레르기 등
  원문의 의미를 보존한다.

#### 공통 오류

```json
{
  "error": {
    "code": "INVALID_DATE_RANGE",
    "message": "시작일은 종료일보다 늦을 수 없습니다.",
    "requestId": "요청 추적 식별자"
  }
}
```

| HTTP 상태 | 의미 |
|-----------|------|
| `400` | 빈 검색어, 잘못된 코드 또는 잘못된 날짜 범위 |
| `404` | 내부 식별자로 학교를 확인할 수 없음 |
| `422` | 형식은 맞지만 계약의 입력 제약을 충족하지 못함 |
| `502` | NEIS가 오류 또는 유효하지 않은 응답을 반환함 |
| `503` | NEIS에 연결할 수 없거나 일시적으로 사용할 수 없음 |
| `504` | NEIS 요청 시간이 초과됨 |

오류 응답은 성공 응답이나 빈 배열로 대체하지 않는다. 사용자가 수정할 수 있는
오류와 재시도 가능한 외부 장애를 안정적인 `code`로 구분한다.

## 6. 데이터 흐름

### 6.1 학교 검색

1. 사용자가 부분 학교명을 입력한다.
2. 프론트엔드가 빈 값 검증과 검색 요청 제어를 수행한다.
3. 프론트엔드 내부 API 클라이언트가 백엔드 학교 검색 엔드포인트를 호출한다.
4. 백엔드가 검색어를 검증한 뒤 NEIS 클라이언트로 학교 기본정보 API를 호출한다.
5. 백엔드가 외부 응답을 검증하고 내부 학교 모델로 변환한다.
6. 프론트엔드가 학교명과 지역 또는 주소를 표시하고 사용자의 선택을 저장한다.

### 6.2 급식 조회

1. 사용자가 학교와 유효한 날짜 범위를 선택한다.
2. 프론트엔드가 내부 급식 조회 엔드포인트를 호출한다.
3. 백엔드가 학교 식별자와 날짜 범위를 검증한다.
4. 백엔드 NEIS 클라이언트가 중식 조건으로 급식식단정보 API를 호출한다.
5. 백엔드가 외부 응답을 검증, 정규화 및 날짜순 정렬한다.
6. 프론트엔드가 날짜별 결과, 빈 상태 또는 오류 상태를 표시한다.

## 7. 런타임 검증과 데이터 처리

- 내부 API의 요청과 응답은 `src/openapi.json`의 타입 및 제약과 일치해야 한다.
- NEIS 응답은 `data/openapi.json`에 따라 백엔드 경계에서 검증한다.
- 날짜는 내부 API에서 `YYYY-MM-DD` 문자열로 전달하고 백엔드에서 실제 달력
  날짜인지 검증한다.
- 외부 응답의 표시용 HTML이나 구분 문자는 허용 목록 기반으로 텍스트 데이터로
  변환해 스크립트나 임의 마크업으로 렌더링하지 않는다.
- 선택적 값의 누락을 빈 문자열이나 `0`으로 바꿔 실제 값처럼 보이게 하지 않는다.
- 외부 응답 원본과 내부 모델의 변환 규칙은 단위 테스트로 고정한다.

## 8. Docker Compose 구성

`compose.yaml`은 최소한 `frontend`, `backend`, `mcp` 서비스를 정의한다.

### frontend 서비스

- React 애플리케이션을 재현 가능한 다단계 이미지로 빌드한다.
- 브라우저에 공개해도 되는 백엔드 기본 경로만 빌드 또는 런타임 설정으로 받는다.
- 호스트에 UI 포트를 공개하고 백엔드 준비 상태에 맞춰 시작 순서를 관리한다.
- NEIS API 키를 환경 변수로 받지 않는다.

### backend 서비스

- Python 애플리케이션과 고정된 의존성을 이미지로 빌드한다.
- 컨테이너 내부의 모든 인터페이스에서 수신하되 필요한 포트만 Compose 네트워크와
  호스트에 노출한다.
- NEIS 기본 URL, API 키, 요청 타임아웃 등 서버 전용 설정을 환경 변수로 받는다.
- 의존 서비스가 실제 요청을 받을 수 있는지 확인하는 헬스 체크를 제공한다.

### mcp 서비스

- `src/mcp/Dockerfile`로 백엔드와 독립된 이미지를 빌드한다.
- `/mcp`와 `/health`를 제공하며 기본 호스트 포트는 `8001`이다.
- NEIS API 키, 기본 URL, 타임아웃, 페이지 크기와 재시도 설정을 환경 변수로 받는다.
- 테스트 Compose에서는 백엔드와 동일한 제어 가능한 NEIS mock을 사용한다.

### 네트워크와 설정

- 서비스 간 통신에는 Compose 서비스 이름을 사용하고 하드코딩한 컨테이너 IP를
  사용하지 않는다.
- 로컬 개발용 예시 환경 파일에는 플레이스홀더만 제공하고 실제 비밀값은 커밋하지
  않는다.
- `docker compose up --build` 한 번으로 두 서비스를 빌드하고 실행할 수 있어야
  하며, `docker compose down`으로 생성된 리소스를 정리할 수 있어야 한다.
- 프론트엔드와 백엔드의 교차 출처 정책은 필요한 프론트엔드 출처만 허용한다.

## 9. 테스트 전략

### 9.1 프론트엔드 통합 테스트

프론트엔드는 컴포넌트 단위 구현 세부사항보다 사용자 흐름 중심의 통합 테스트를
작성한다. 내부 API는 계약에 맞는 테스트 대역으로 제어한다.

- 부분 학교명 입력, 검색 결과 표시 및 학교 선택
- 검색 결과 없음과 검색 실패의 서로 다른 안내
- 학교 선택 전 날짜 단계 비활성화
- 유효하거나 잘못된 날짜 범위의 검증
- 중식 결과의 날짜순 표시와 선택적 필드 누락
- 급식 정보 없음, 로딩, 재시도 가능한 오류 상태
- 늦게 도착한 이전 검색 응답이 최신 결과를 덮지 않는 동작
- 키보드 조작 및 상태 안내의 핵심 접근성

별도의 프론트엔드 단위 테스트 묶음은 요구하지 않는다.

### 9.2 백엔드 단위 테스트

- 검색어 및 날짜 범위 검증
- NEIS 학교 및 급식 응답에서 내부 모델로의 변환
- 메뉴와 선택적 영양 정보의 안전한 정규화
- 중식 조건, 학교 식별자 및 날짜 매개변수 구성
- 외부 오류 코드, 유효하지 않은 본문, 타임아웃과 연결 실패의 오류 매핑
- 페이지네이션, 정렬 및 중복 처리
- 재시도 가능한 오류와 재시도하면 안 되는 오류의 구분

### 9.3 백엔드 통합 테스트

실제 외부 서비스에 의존하지 않는 제어된 NEIS 모의 서버와 백엔드 HTTP 서버를
함께 실행한다.

- `src/openapi.json`에 정의된 성공 및 오류 응답 준수
- `data/openapi.json`에 해당하는 NEIS 요청 경로와 쿼리 생성
- 검색 결과 없음과 급식 결과 없음의 `200` 빈 배열 처리
- 잘못된 입력의 외부 호출 차단
- NEIS 장애, 타임아웃 및 잘못된 응답의 `5xx` 매핑
- 환경 변수 누락 시 명확한 시작 실패

### 9.4 E2E 테스트

Docker Compose로 프론트엔드, 백엔드 및 제어 가능한 NEIS 대역을 실행하고
브라우저에서 전체 흐름을 검증한다.

1. 부분 학교명으로 검색한다.
2. 동명의 학교를 구분해 한 학교를 선택한다.
3. 시작일과 종료일을 선택한다.
4. 조회한 중식이 날짜순으로 표시되는지 확인한다.
5. 날짜 범위를 바꿔 결과가 갱신되는지 확인한다.
6. 검색 결과 없음, 급식 정보 없음 및 외부 장애 시나리오를 각각 확인한다.

E2E 테스트는 프론트엔드가 NEIS 대역을 직접 호출하지 않고 반드시 백엔드를
경유하는지도 검증한다. 실제 NEIS 데이터나 운영 API의 가용성에 테스트 성공
여부를 의존시키지 않는다.

### 9.5 MCP 테스트

- 도구 목록과 입력 스키마가 MCP 서버에 등록되는지 검증한다.
- 학교 검색 및 중식 도구가 NEIS 계약에 맞는 쿼리를 생성하는지 검증한다.
- 정상 구조화 결과와 입력 오류, 결과 없음, 잘못된 응답, 연결 실패 및 시간 초과
  도구 오류를 검증한다.
- 테스트는 mock transport를 사용하고 실제 API 키나 NEIS 가용성에 의존하지 않는다.

### 9.6 계약 및 실행 검사

- 내부 API 구현과 프론트엔드 클라이언트가 `src/openapi.json`과 일치하는지
  자동 검사한다.
- NEIS 클라이언트의 요청과 응답 모델이 `data/openapi.json`과 일치하는지
  통합 테스트로 확인한다.
- `docker compose config`로 Compose 구성을 검증하고, 빌드 후 헬스 체크와 E2E
  테스트를 실행한다.

## 10. 관측성과 오류 처리

- 각 백엔드 요청에 추적 식별자를 부여하고 오류 응답과 구조화 로그에 포함한다.
- 로그에는 내부 오류 코드, 외부 상태 분류, 처리 시간 등 진단 정보를 남긴다.
- API 키, 전체 인증 헤더 및 불필요한 외부 응답 원문은 기록하지 않는다.
- 프론트엔드는 사용자에게 안정적인 메시지를 표시하고 서버 내부 상세나 비밀값을
  노출하지 않는다.

## 11. 완료 기준

- React 프론트엔드와 Python 백엔드의 책임이 분리되어 있다.
- 프론트엔드 네트워크 요청은 `src/openapi.json`의 백엔드 API로만 향한다.
- 백엔드 NEIS 클라이언트가 `data/openapi.json`에 따라 요청하고 응답을 검증한다.
- Docker Compose로 프론트엔드, 백엔드와 독립 MCP 서버를 함께 실행할 수 있다.
- 프론트엔드 통합 테스트, 백엔드 단위 및 통합 테스트, 전체 사용자 흐름 E2E
  테스트가 정상, 빈 결과, 입력 오류 및 외부 장애를 검증한다.
